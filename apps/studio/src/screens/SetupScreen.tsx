// ── Setup screen (legacy) ───────────────────────────────────────────
// Replaced by BootingScreen in Phase 2. Kept as re-export for compatibility.

export { BootingScreen as SetupScreen } from './BootingScreen';
